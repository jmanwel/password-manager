# DOC

Script to encrypt/decryt text

## Arguments

* "--action", "-a"  => Action to perform.
* "--password", "-pwd"  => Password.
* "--method", "-m"  => sha256, sha512, etc
* "--path", "-p"  => Path to dictionary
* "--hashed", "-ha" => Hash to decode